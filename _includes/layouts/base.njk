<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

	{% include "components/head.njk" %}

	<body>

		<header class="header">
			<a href="{{ '/' | url }}">
				<img class="header__img" src="/_includes/assets/img/logo.jpg" alt="{{ metadata.title }}" />
			</a>
			{% include "components/nav.njk" %}
		</header>

		<main>
			{{ layoutContent | safe }}
		</main>

		{% include "components/footer.njk" %}

	</body>

</html>
